# 安全说明

- 密码使用 BCrypt 哈希存储，登录时通过 `PasswordEncoder` 校验。
- JWT 只保存用户 ID、用户名、用户类型和角色，不保存密码、身份证号、数据库连接信息等敏感字段。
- 数据库连接密码通过环境变量或本地配置覆盖，不在仓库配置中硬编码。
- 权限以后端 Spring Security 和 Service 层数据范围校验为准，前端只根据 `/api/auth/me` 和 `/api/auth/menus` 返回结果控制显示。
- 数据库不使用外键，关联字段均建立索引，并在 Service 层校验引用存在性与权限范围。
